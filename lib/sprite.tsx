import React from 'react';

export interface ISpriteProps {
    //children?: React.ReactNode,
}

const Sprite = (
    {}: ISpriteProps
) => {

    return (
        <div>
        </div>
    );
};

export default Sprite;