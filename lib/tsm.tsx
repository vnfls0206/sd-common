import React from 'react';

export interface ITsmProps {
    //children?: React.ReactNode,
}

const Tsm = (
    {}: ITsmProps
) => {

    return (
        <div>
        </div>
    );
};

export default Tsm;