import React from 'react';

export interface ILllProps {
    //children?: React.ReactNode,
}

const Lll = (
    {}: ILllProps
) => {

    return (
        <div>
        </div>
    );
};

export default Lll;