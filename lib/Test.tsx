import React from 'react';

export interface ITestProps {
    //children?: React.ReactNode,
}

const Test = (
    {}: ITestProps
) => {

    return (
        <div>
        </div>
    );
};

export default Test;