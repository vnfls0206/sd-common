import React from 'react';

export interface IFiveProps {
    //children?: React.ReactNode,
}

const Five = (
    {}: IFiveProps
) => {

    return (
        <div>
        </div>
    );
};

export default Five;