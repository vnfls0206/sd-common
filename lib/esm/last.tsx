import React from 'react';

export interface ILastProps {
    //children?: React.ReactNode,
}

const Last = (
    {}: ILastProps
) => {

    return (
        <div>
        </div>
    );
};

export default Last;