import React from 'react';

export interface ITttProps {
    //children?: React.ReactNode,
}

const Ttt = (
    {}: ITttProps
) => {

    return (
        <div>
        </div>
    );
};

export default Ttt;