import React from 'react';

export interface ISixProps {
    //children?: React.ReactNode,
}

const Six = (
    {}: ISixProps
) => {

    return (
        <div>
            </div>
    );
};

export default Six;